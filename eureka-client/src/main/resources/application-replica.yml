server:
  port: 8102
spring:
  application:
    name: eureka-client

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://replica1:8002/eureka/, http://replica1:8003/eureka/